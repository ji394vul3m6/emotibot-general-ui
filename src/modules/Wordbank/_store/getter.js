export const wordbank = s => s.wordbank;
export const currentCategory = s => s.currentCategory;
export const isEditMode = s => s.isEditMode;
export const hasCategoryNameEditing = s => s.hasCategoryNameEditing;
